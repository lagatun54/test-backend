{
    "name": "mock/backend",
    "description": "Minimal PHP 8 mock backend API",
    "type": "project",
    "require": {
      "php": "^8.0"
    },
    "autoload": {
      "psr-4": {
        "App\\": "src/"
      }
    },
    "scripts": {
      "start": "php -S 0.0.0.0:${PORT:-8080} -t public"
    }
  }